# "viewer" keeps the top-right menu (Settings/theme, Print, About) for end users
# but hides developer-only chrome such as the Deploy button.
[client]
toolbarMode = "viewer"

[browser]
gatherUsageStats = false
