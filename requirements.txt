# ---------------------------------------------------------------------------
# RUNTIME dependencies for the Streamlit dashboard ONLY — no GDAL / geo stack.
#
# The dashboard serves the pre-built artefacts in data/processed/ and never
# opens a raster, so it does not need rasterio/geopandas/shapely. Keeping this
# list slim makes cloud deploys fast and reliable.
#
# The FULL pipeline dependencies (rasterio, geopandas, …) live in pyproject.toml
# and are only needed locally to (re)build the data.
# ---------------------------------------------------------------------------
streamlit>=1.40,<2
plotly>=5.24          # go.Choroplethmap (MapLibre) requires >=5.24
pandas>=2.2
pyarrow>=15
numpy>=1.26
pyyaml>=6.0
