# Python
__pycache__/
*.py[cod]
.venv/
*.egg-info/
.pytest_cache/
.ipynb_checkpoints/

# Raw raster/boundary cache — large, rebuildable, never committed.
data/raw/

# NOTE: data/processed/ is intentionally COMMITTED. The artefacts are tiny
# (~1 MB total) and let the dashboard deploy to Streamlit Cloud without running
# the heavy geo pipeline on the host. Rebuild them anytime with `make pipeline`.

# OS / editor
.DS_Store
.vscode/
.idea/

# Claude Code session artefacts
.claude/
